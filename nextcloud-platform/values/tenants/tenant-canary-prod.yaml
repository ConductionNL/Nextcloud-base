---
tenant:
  name: canary-prod
  environment: prod
  wave: "0"
  dbType: mariadb
  apps:
    enabled:
      - opencatalogi
      - openconnector
      - openregister

# Canary: test RS=2 HA here before rolling to prod tenants
replicaCount: 2
